#!/usr/bin/env ruby

$LIB_DIR = File.join(File.dirname(__FILE__), "..", "lib")
$LOAD_PATH << $LIB_DIR 

require "collective"
require "command"

PLAYPEN = ENV["PLAYPEN"] || "network"

puts "/// using '#{PLAYPEN}' playpen file"

load PLAYPEN

command = Command.new(@subnet)
command.run(*ARGV)
