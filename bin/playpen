#!/usr/bin/env ruby

$LIB_DIR = File.join(File.dirname(__FILE__), "..", "lib")
$LOAD_PATH << $LIB_DIR 

require "playpen"
require "command"

PLAYPEN = ENV["PLAYPEN"] || "network.lab"

puts "/// using '#{PLAYPEN}' laboratory file"

load PLAYPEN

command = Command.new(@subnet)
command.run(*ARGV)
