#!/usr/bin/env ruby

$LIB_DIR = File.join(File.dirname(__FILE__), "..", "lib")
$LOAD_PATH << $LIB_DIR 

require "collective"
require "command"

load "network"

command = Command.new(@subnet)
command.run(*ARGV)
